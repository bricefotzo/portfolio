# ── SmartCity Explorer — Configuration ──────────────────────────
# Copiez ce fichier en .env et remplissez vos valeurs

# ── PostgreSQL (Neon / local) ──────────────────────────────────
POSTGRES_URL=postgresql+asyncpg://user:password@host:5432/smartcity
# Ou variables séparées :
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=smartcity
# POSTGRES_USER=user
# POSTGRES_PASSWORD=password

# ── MongoDB (Atlas / local) ────────────────────────────────────
MONGO_URL=mongodb+srv://user:password@cluster.mongodb.net/smartcity
MONGO_DB=smartcity

# ── Neo4j (Aura / local) ──────────────────────────────────────
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# ── Backend ────────────────────────────────────────────────────
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# ── Frontend ───────────────────────────────────────────────────
API_BASE_URL=http://localhost:8000
